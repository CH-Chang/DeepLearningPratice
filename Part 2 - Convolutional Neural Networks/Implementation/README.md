# Implementation For Learning RNN

## 錯誤修正

- Tensorflow 安裝問題 ([參考來源](https://stackoverflow.com/questions/74949556/poetry-fails-to-install-tensorflow))

1. 進入poetry shell

   ```bash
   poetry shell
    ```

2. 重新安裝tensorflow

   ```bash
   pip install tensorflow==2.12.0 --force-install
   ```
